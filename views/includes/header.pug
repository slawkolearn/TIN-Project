header 
  h1 
    a(href='/')
      img.mainAppLogo(src="/static/img/logo.png", alt="Logo Strony PicAppSite")
  if !error
    a.homePageLink(href=`/`) Strona Główna
  if message
    include message.pug